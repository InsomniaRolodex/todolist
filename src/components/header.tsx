import { JSX } from "react";

const Header = (): JSX.Element => {
    return (
        <header className="page-header">
            <h1 className="page-header__text">This is todo list!</h1>
        </header>
    )
}

export default Header;